- name: Deploy mha-go HA manager on a dbbot master-slave MySQL cluster
  hosts: dbbot_mysql
  become: true
  any_errors_fatal: true
  max_fail_percentage: 1
  vars_files:
    - default/common_config.yml
    - default/advanced_config.yml
    - default/var_mha_go.yml
    - common_config.yml
    - advanced_config.yml
    - vars/var_mha_go.yml
  vars:
    dbbot_entry_playbook_name: "mha_go.yml"
    roles_to_execute:
      - "../roles/pre_check_and_set"
      - "../roles/make_mha_go"
    mtls_with_mysql_group_replication: 0
  pre_tasks:
    - name: Import tasks to validate setting in common_config.yml
      ansible.builtin.import_tasks: pre_tasks/validate_common_config_setting.yml

    - name: Import tasks to check os_type and python
      ansible.builtin.import_tasks: pre_tasks/check_os_and_python.yml

    - name: Import tasks to validate network interfaces for VIP-related roles
      ansible.builtin.import_tasks: pre_tasks/validate_network_interfaces_for_vip_roles.yml

    - name: Import validate mha_go tasks
      ansible.builtin.import_tasks: pre_tasks/validate_mha_go.yml

    - name: Import confirmation tasks
      ansible.builtin.import_tasks: pre_tasks/confirmation.yml

  tasks:
    - name: Include role
      ansible.builtin.include_role:
        name: "{{ role_name }}"
      loop: "{{ roles_to_execute }}"
      loop_control:
        loop_var: role_name

  post_tasks:
    - name: Run dbbot post-deploy checks
      ansible.builtin.import_tasks: post_tasks/run_post_checks.yml
