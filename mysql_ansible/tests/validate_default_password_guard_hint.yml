- name: Verify default password guard suggests the explicit allow override
  hosts: localhost
  connection: local
  gather_facts: false
  vars_files:
    - ../playbooks/default/common_config.yml
  vars:
    ansible_python_interpreter: "{{ ansible_playbook_python }}"
    dbbot_entry_playbook_name: backup_script_8.4.yml
  tasks:
    - name: Import common config validation
      ansible.builtin.import_tasks: ../playbooks/pre_tasks/validate_common_config_setting.yml
