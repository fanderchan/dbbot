#!/bin/bash
# Keepalived notify hook for MySQL Router HA

LOG_FILE="{{ router_health_log_file | default('/var/log/keepalived/check_mysqlrouter.log') }}"

timestamp="$(date '+%Y-%m-%d %H:%M:%S')"
echo "${timestamp} $*" >> "${LOG_FILE}"
