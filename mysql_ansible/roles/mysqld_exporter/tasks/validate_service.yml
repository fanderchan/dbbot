- name: Check mysqld_exporter service is operational
  ansible.builtin.uri:
    url: "http://localhost:{{ mysqld_exporter_port_resolved | int }}/metrics"
    method: GET
    return_content: yes
  register: result
  until: result.status == 200
  retries: 5
  delay: 10
  when: mysqld_exporter_install
