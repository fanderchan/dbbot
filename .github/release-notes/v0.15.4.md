## [Change]

- Display the MySQL Router HA deployment summary on each Router node so asymmetric network-interface deployments show the interface that applies to that host.

## [Fix]

- Document the tested MySQL Router HA configuration pattern for Router nodes that use different network interface names.
- Clarify MySQL uninstall examples so operators pass both the deployed MySQL version and the target instance port.
