## [Note]

dbbot v0.14.1 is a documentation and release metadata patch release. It does not change MySQL playbook runtime behavior from v0.14.0.

## [Change]

- Clarify MySQL official test environment guidance for the `192.168.161.*` lab network versus reserved `192.0.2.*` example defaults.
- Update packaged version metadata to `0.14.1`.
