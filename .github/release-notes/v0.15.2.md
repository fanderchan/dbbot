## [Change]

- Document that minimal IaaS control nodes must provide `tar` before extracting a dbbot release package.

## [Fix]

- Fix master-slave and InnoDB Cluster deployments on mixed operating system labs by disabling the network-interface alias uniformity check for non-VIP topologies.
