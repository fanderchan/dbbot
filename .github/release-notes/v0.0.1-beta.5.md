dbbot v0.0.1-beta.5 rebuilds the managed release package through a temporary staging directory before creating the final tarball.

This keeps the packaging flow easier to audit: the release script now copies the allowed repository content into a clean dbbot staging tree and then archives that tree with a plain tar invocation.

The resulting package preserves relative symlinks inside portable-ansible correctly and still records normalized numeric ownership metadata for the curated release asset.
