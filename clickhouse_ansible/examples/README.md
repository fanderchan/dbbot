# Examples

`examples/` 保存的是手工演练和示例数据工具，不是核心部署入口，也不是自动化测试目录。

当前包含：

1. `hot_backup_lab/`：备份/恢复演练造数、持续写入和验收 SQL
2. `business_demo/`：业务风格模拟数据生成脚本

约定：

1. `examples/*/run/`、日志、PID 文件都属于运行时产物，不应提交。
2. 示例中的 IP、密码和集群名均为公开占位值，使用前请替换。
